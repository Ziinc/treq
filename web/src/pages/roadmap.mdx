---
title: Roadmap
description: Upcoming milestones for Treq
hide_table_of_contents: true
wrapperClassName: roadmap-page
---

import ContentPageLayout from '@site/src/components/ContentPageLayout';
import RoadmapGraphicTabs from '@site/src/components/RoadmapGraphicTabs';

export const toc = [
  { id: '2026', value: '2026', level: 2 },
  { id: 'github-integration-with-merge-queue', value: 'GitHub Integration with Merge Queue', level: 2 },
  { id: 'workspace-checks', value: 'Workspace Checks', level: 2 },
  { id: 'ssh-remote-development', value: 'SSH Remote Development', level: 2 },
  { id: 'how-these-fit-together', value: 'How These Fit Together', level: 2 },
];

<ContentPageLayout toc={toc} breadcrumbs={[{label: 'Home', href: '/'}, {label: 'Roadmap'}]}>

# Roadmap

_Year-by-year product milestones for Treq. Newer years are added above older ones._

## 2026

<RoadmapGraphicTabs version="2026" />

Q1 and Q2 have nothing planned. GitHub pull requests, CI status, and inline review threads shipped in [0.2.0](/changelog). The Treq-managed merge queue is still in progress. Q3 also targets workspace checks. Q4 targets SSH remote development.

## GitHub Integration with Merge Queue

:::tip Shipped in v0.2.0

Pull request create and view, CI status, and inline GitHub review threads are available now. See [GitHub Integration](/docs/concepts/github-integration).

:::

:::note Work in progress

The Treq-managed merge queue has UI and backend foundations in the app, but it is not finished. Treat queue docs as a preview of the intended design, not a complete product promise.

:::

[Treq workspaces](/docs/concepts/workspaces) model stacked branches. The GitHub integration connects each workspace to its remote pull request from the same place you review diffs and drive agents.

**App integration** links a workspace to its PR. You see PR state on the sidebar icon and checks in the workspace header. Creating a pull request from a workspace pushes the branch when needed and opens the PR against the stack parent base when the workspace targets another branch, matching the [stacked PR](/learn/concepts/git/stacked-prs) model.

**Merge queue integration** is still WIP. The planned design is a Treq-managed merge queue, not [GitHub's native merge queue](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/configuring-pull-request-merges/managing-a-merge-queue). GitHub's built-in queue requires GitHub Enterprise Cloud for private repositories. Treq's queue will not. The goal is to enqueue stacked PRs the way you already manage stacks locally, with dependency order so a higher layer does not jump ahead of its required base.

Setup for the shipped PR and CI surfaces is covered in [Connecting GitHub](/docs/how-to/connecting-github) and [Creating and Viewing Pull Requests](/docs/how-to/creating-and-viewing-pull-requests). The [merge queue how-to](/docs/how-to/using-the-merge-queue) describes the intended queue workflow while that work finishes.

## Workspace Checks

[Coding agents](/learn/concepts/ai-engineering/coding-agents) produce plausible changes. **Workspace Checks** make those changes accountable. You define workflows, similar in shape to GitHub Actions, that run as quality gates inside a workspace before you treat the work as ready to push or merge.

A check workflow is a named set of jobs and steps: install, lint, typecheck, unit tests, integration tests, or any shell-backed assertion you care about. Each step passes or fails. Soft advice is not enough here. The gate is the assertion result.

The longer-term goal is an **agentic self-fixing loop**. An agent makes a change, runs the workspace checks, reads the failures, and retries until the gates pass or it stops with a clear failure. Strict assertion-based guardrails keep that loop grounded in observable outcomes instead of the model's confidence. Full agentic self-fixing loops are a stretch goal and are not scheduled for the initial feature release. The first ship is the check gates themselves: run them, see pass or fail, and decide what to do next.

These checks live with the workspace, so each stacked layer can verify its own increment before it joins a larger review or merge queue.

## SSH Remote Development

Today, [Treq workspaces](/docs/concepts/workspaces) and [terminal sessions](/docs/concepts/terminal-sessions) run on your local machine. **SSH Remote Development** opens a workspace on a remote host over SSH and lets you develop there directly from the same UI.

You connect to a remote machine, attach to a repository path there, and get the same stacked workspace model: diffs, commits, agents, and checks against that remote working copy. Shell and agent processes run on the remote host. File reads and writes go over the SSH session instead of the local disk.

This matters when the project needs a machine you do not run locally. Examples include more CPU or GPU, a shared build environment, or services that only exist on that host. The goal is one Treq session that feels like local development while the workspace lives elsewhere.

## How These Fit Together

Workspace Checks decide whether a layer is ready. [GitHub Integration](/docs/concepts/github-integration) already opens and tracks the remote PR path. The merge queue will move ready layers onto an automated merge path when that work finishes. SSH Remote Development decides where that work runs when local hardware is not enough.

You build in a stacked workspace, locally or over SSH, and run checks until the assertions pass. Then you open or update the PR from Treq. When the queue ships, you will be able to enqueue that PR alone or with the rest of the stack. The same verification story that protects local agent loops should also protect what you put into the queue.

## Learn More

- [GitHub Integration](/docs/concepts/github-integration)
- [Using the Merge Queue](/docs/how-to/using-the-merge-queue)
- [Workspaces](/docs/concepts/workspaces)
- [Terminal Sessions](/docs/concepts/terminal-sessions)
- [Stacked PRs](/learn/concepts/git/stacked-prs)
- [Stacked PR Workflow](/learn/workflows/git/stacked-pr)
- [Coding Agents](/learn/concepts/ai-engineering/coding-agents)
- [Changelog](/changelog)

</ContentPageLayout>
