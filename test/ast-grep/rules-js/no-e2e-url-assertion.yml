id: no-e2e-url-assertion
language: typescript
severity: error
message: >
  Do not assert on URLs in e2e tests.
  Assert on visible page content (headings, text) instead of toHaveURL().
files:
  - "web/e2e/**/*.ts"
rule:
  pattern: $_.toHaveURL($$$)
