id: no-command-for-jj-or-git-tauri-src
snapshots:
  ? |
    fn status() {
      let mut cmd = command_for("jj");
      cmd.arg("status");
    }
  : labels:
    - source: command_for("jj")
      style: primary
      start: 30
      end: 47
  ? |
    fn status() {
      let mut cmd = command_for("git");
      cmd.arg("status");
    }
  : labels:
    - source: command_for("git")
      style: primary
      start: 30
      end: 48
