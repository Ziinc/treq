id: prefer-use-optimistic
snapshots:
  ? |
    const like = async () => {
      const previous = likes;
      setLikes(likes + 1);
      try {
        await saveLike(likes + 1);
      } catch (err) {
        setLikes(previous);
      }
    };
  : labels:
    - source: const previous = likes;
      style: primary
      start: 29
      end: 52
