id: prefer-use-form-status
snapshots:
  ? |
    export const MergeDialog = ({ isSubmitting, onConfirm }) => (
      <button disabled={isSubmitting}>Confirm</button>
    );
  : labels:
    - source: isSubmitting
      style: primary
      start: 30
      end: 42
  ? |
    interface MergeDialogProps {
      isSubmitting: boolean;
      onConfirm: () => void;
    }
  : labels:
    - source: isSubmitting
      style: primary
      start: 31
      end: 43
